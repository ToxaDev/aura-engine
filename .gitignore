# Rust build artifacts
/desktop-app/src-tauri/target

# Python
__pycache__/
*.pyc
*.pyo

# Python virtual environments
/fir-optimizer/venv
/fir-optimizer/.venv

# Generated filter blobs (large binary .npy — regenerate via fir-optimizer/optimize.py)
/fir-optimizer/output
/fir-optimizer/verify_results

# Packaging output — the bundles built by tools/make-bundles.ps1 (up to ~1.5 GB)
/dist

# Node / npm
node_modules/

# Editor / local tooling
.vscode/
.idea/

# Local-only working notes — never published
/*.local.md
